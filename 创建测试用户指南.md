# 🔐 创建测试用户指南

## 问题分析

登录失败的原因可能是：**数据库中还没有用户数据**。

登录流程：
1. 前端发送登录请求到后端
2. 后端首先尝试从**数据库**验证用户
3. 如果数据库中没有用户，在开发环境下会回退到**白名单验证**
4. 如果白名单验证也失败，返回错误

## ✅ 解决方案：在数据库中创建用户

### 方法 1：使用脚本创建（推荐）

在服务器上执行：

```bash
cd /opt/ue-assets-backend/backend-api
./scripts/create-test-user.sh
```

或者直接运行：

```bash
cd /opt/ue-assets-backend/backend-api
npx ts-node scripts/create-test-user.ts
```

这会创建以下测试用户：
- `test@factory-buy.com` / `password123`
- `admin@admin.local` / `admin123`

### 方法 2：通过 API 注册用户

如果后端服务正在运行，可以通过 API 注册：

```bash
# 注册 test@factory-buy.com
curl -X POST http://localhost:3002/users/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@factory-buy.com",
    "password": "password123",
    "name": "测试用户"
  }'

# 注册 admin@admin.local
curl -X POST http://localhost:3002/users/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@admin.local",
    "password": "admin123",
    "name": "管理员"
  }'
```

### 方法 3：手动在数据库中创建

如果可以直接访问数据库：

```sql
-- 连接到数据库
psql -h localhost -U ue_user -d ue_assets

-- 创建用户（需要先加密密码）
-- 注意：密码需要使用 bcrypt 加密，建议使用方法 1 或 2
```

## 🔍 验证用户是否创建成功

### 检查数据库中的用户

```bash
# 在服务器上，如果可以直接访问数据库
psql -h localhost -U ue_user -d ue_assets -c "SELECT email, name, credits FROM users;"
```

### 通过 API 检查

```bash
# 需要管理员权限，先登录获取 token
curl -X GET http://localhost:3002/users/list \
  -H "Authorization: Bearer YOUR_TOKEN"
```

## 📝 登录账号（创建后）

创建用户后，可以使用以下账号登录：

### 账号 1：test@factory-buy.com

**用户名/邮箱**：`test@factory-buy.com`  
**密码**：`password123`

### 账号 2：admin@admin.local

**用户名/邮箱**：`admin@admin.local`  
**密码**：`admin123`

**注意**：如果前端输入的是 `admin`，系统会自动转换为 `admin@admin.local`。

## 🚀 快速操作步骤

在服务器上执行：

```bash
# 1. 进入后端目录
cd /opt/ue-assets-backend/backend-api

# 2. 创建测试用户
npx ts-node scripts/create-test-user.ts

# 3. 验证用户创建成功（如果可以直接访问数据库）
psql -h localhost -U ue_user -d ue_assets -c "SELECT email, name FROM users;"
```

创建用户后，就可以正常登录了！


