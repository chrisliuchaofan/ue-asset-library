# 🔐 测试账号配置

## ✅ 可用的测试账号

根据当前配置，以下账号应该可以登录：

### 账号 1：test@factory-buy.com（推荐）

**用户名/邮箱**：`test@factory-buy.com`  
**密码**：`password123`

这个账号在后端 `USER_WHITELIST` 中已配置，应该可以直接登录。

### 账号 2：admin（如果配置正确）

**用户名**：`admin`  
**密码**：`admin123`

**注意**：登录时系统会自动将 `admin` 转换为 `admin@admin.local`，所以后端需要配置 `admin@admin.local:admin123`。

## 🔧 配置检查

### 检查前端配置

```bash
cd /Users/chrisl/Documents/恒星UE资产库/web
grep ADMIN_USERS .env.local
```

应该看到：
```
ADMIN_USERS=admin:admin123
```

### 检查后端配置（服务器上）

```bash
cd /opt/ue-assets-backend/backend-api
grep USER_WHITELIST .env
```

应该看到：
```
USER_WHITELIST=test@factory-buy.com:password123,admin@admin.local:admin123
```

## 🚀 快速测试

### 使用 test@factory-buy.com 登录（最可靠）

1. 访问 `http://localhost:3000/auth/login`
2. 输入用户名：`test@factory-buy.com`
3. 输入密码：`password123`
4. 点击登录

### 如果还是不行

检查后端服务是否正常运行：
```bash
curl http://localhost:3002/health
# 或服务器上
curl http://localhost:3002/health
```

## 📝 添加新测试账号

如果需要添加新账号：

### 前端配置（.env.local）

```env
ADMIN_USERS=admin:admin123,test@factory-buy.com:password123,新用户名:新密码
```

### 后端配置（服务器 .env）

```env
USER_WHITELIST=test@factory-buy.com:password123,admin@admin.local:admin123,新邮箱:新密码
```

**注意**：后端需要完整的邮箱格式（包含 @）。







