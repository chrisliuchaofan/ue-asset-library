# 🖥️ 服务器部署指南

## 📍 在服务器上找到项目路径

在 Linux 服务器上，项目路径通常不在 `/Users/` 下（那是 macOS 路径）。

### 常见项目位置

1. **用户主目录下**：
   ```bash
   cd ~/web/backend-api
   # 或
   cd /root/web/backend-api
   ```

2. **/opt 目录下**：
   ```bash
   cd /opt/web/backend-api
   ```

3. **/var/www 目录下**：
   ```bash
   cd /var/www/web/backend-api
   ```

4. **/home 目录下**：
   ```bash
   cd /home/用户名/web/backend-api
   ```

### 如何找到项目路径

在服务器上执行以下命令查找项目：

```bash
# 方法 1：查找 package.json 文件
find / -name "package.json" -path "*/backend-api/*" 2>/dev/null

# 方法 2：查找 backend-api 目录
find / -type d -name "backend-api" 2>/dev/null

# 方法 3：查找项目相关文件
find / -name "*.ts" -path "*/backend-api/src/*" 2>/dev/null | head -1

# 方法 4：检查当前用户主目录
ls -la ~/
ls -la ~/web/
```

## 🚀 启动后端服务

找到正确的路径后：

```bash
# 进入后端目录
cd /找到的路径/backend-api

# 检查是否有 node_modules
ls -la node_modules/

# 如果没有，先安装依赖
npm install

# 启动服务
npm run start:dev
```

## 📝 检查环境变量

确保 `.env` 文件存在且配置正确：

```bash
cd /找到的路径/backend-api
cat .env | grep USER_WHITELIST
```

应该看到：
```
USER_WHITELIST=test@factory-buy.com:password123,admin@admin.local:admin123
```

## ⚠️ 注意事项

1. **路径差异**：macOS 和 Linux 的路径不同
   - macOS: `/Users/chrisl/Documents/...`
   - Linux: `/root/...` 或 `/home/用户名/...`

2. **权限问题**：确保有读取和执行的权限

3. **Node.js 版本**：确保服务器上安装了 Node.js
   ```bash
   node --version
   npm --version
   ```


