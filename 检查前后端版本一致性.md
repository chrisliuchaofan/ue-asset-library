# 检查前后端版本一致性

## 📋 检查方法

### 方法 1：通过 Git Commit Hash

**前端（本地）：**
```bash
cd /Users/chrisl/Documents/恒星UE资产库/web
git log --oneline -1
```

**后端（服务器）：**
```bash
# SSH 连接到服务器
ssh root@your-server-ip
cd /opt/ue-assets-backend/backend-api
git log --oneline -1
```

**比较：** 如果 commit hash 相同，说明版本一致。

---

### 方法 2：通过 Vercel 和 GitHub Actions

**前端版本：**
1. 访问：`https://vercel.com/chrisliuchaofan/ue-asset-library/deployments`
2. 查看最新的部署记录
3. 查看 "Source" 或 "Commit" 信息

**后端版本：**
1. 访问：`https://github.com/chrisliuchaofan/ue-asset-library/actions`
2. 查看最新的 "Deploy Backend to ECS" 工作流
3. 查看 "Commit" 信息

**比较：** 如果两个 commit hash 相同，说明版本一致。

---

### 方法 3：通过 API 版本信息（如果已实现）

**前端：**
```bash
# 查看前端构建信息（如果有）
curl https://factory-buy.com/api/version
```

**后端：**
```bash
# 查看后端版本信息（如果有）
curl https://api.factory-buy.com/health
```

---

## 🔍 当前状态检查

### 检查本地代码版本
```bash
cd /Users/chrisl/Documents/恒星UE资产库/web
git log --oneline -5
```

### 检查服务器后端版本
```bash
# 需要 SSH 连接到服务器
ssh root@your-server-ip
cd /opt/ue-assets-backend/backend-api
git log --oneline -5
```

---

## ⚠️ 注意事项

1. **前端部署延迟：** Vercel 部署可能需要几分钟
2. **后端部署延迟：** GitHub Actions 部署可能需要 5-10 分钟
3. **缓存问题：** 浏览器可能缓存旧版本，需要强制刷新（Ctrl+F5）

---

## 🛠️ 如果版本不一致

### 前端版本落后
- 检查 Vercel 部署状态
- 手动触发部署（如果需要）

### 后端版本落后
- 检查 GitHub Actions 部署状态
- 查看部署日志，确认是否有错误
- 手动触发部署（如果需要）

---

**建议：** 每次部署后，记录前后端的 commit hash，方便后续对比。


