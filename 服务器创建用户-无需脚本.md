# 🔐 在服务器上创建用户（无需脚本文件）

## ✅ 方法 1：通过 API 注册（推荐，最简单）

确保后端服务正在运行（端口 3002），然后在服务器终端执行：

### 注册 test@factory-buy.com

```bash
curl -X POST http://localhost:3002/users/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@factory-buy.com","password":"password123","name":"测试用户"}'
```

### 注册 admin@admin.local

```bash
curl -X POST http://localhost:3002/users/register \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@admin.local","password":"admin123","name":"管理员"}'
```

### 预期输出

如果成功，会返回类似：
```json
{
  "user": {
    "id": "test@factory-buy.com",
    "email": "test@factory-buy.com",
    "name": "测试用户",
    "credits": 100,
    ...
  },
  "token": "eyJhbGc..."
}
```

## 🔍 验证用户是否创建成功

### 方法 1：尝试登录

在前端使用创建的账号登录：
- `test@factory-buy.com` / `password123`
- `admin@admin.local` / `admin123`（前端输入 `admin`）

### 方法 2：检查数据库（如果可以直接访问）

```bash
# 如果可以直接访问数据库
psql -h localhost -U ue_user -d ue_assets -c "SELECT email, name, credits FROM users;"
```

## ⚠️ 如果 API 注册失败

可能的原因：
1. 后端服务没有运行
2. 用户已存在（会返回 409 错误）
3. 数据库连接问题

### 检查后端服务

```bash
# 检查服务是否运行
curl http://localhost:3002/health

# 检查服务日志
# 在运行服务的终端查看日志
```

## 📝 完整操作步骤

在服务器终端依次执行：

```bash
# 1. 检查后端服务是否运行
curl http://localhost:3002/health

# 2. 注册第一个用户
curl -X POST http://localhost:3002/users/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@factory-buy.com","password":"password123","name":"测试用户"}'

# 3. 注册第二个用户
curl -X POST http://localhost:3002/users/register \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@admin.local","password":"admin123","name":"管理员"}'

# 4. 验证（尝试登录或检查数据库）
```

## 🎯 创建后可以使用的账号

1. **test@factory-buy.com** / **password123**
2. **admin@admin.local** / **admin123**（前端输入 `admin` 会自动转换）

创建成功后，就可以在前端正常登录了！







